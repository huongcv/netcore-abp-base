import {ColSize} from "antd/es/grid/col";

export interface ISearchProp {
    span?: number;
    md?: number | ColSize;
    lg?: number | ColSize;
    xl?: number | ColSize;
    xxl?: number | ColSize;
}
